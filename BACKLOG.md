# 개선 백로그 (상시 기록)

> 원칙: 검증에서 이긴 것만 적용. 상승장 표본만으로 판단 금지. 기존 검증된 동작은 기본값으로 보존.
> 과적합 방지 규율: ① 가설을 먼저 적고 스윕은 그 검증만 ② 단일 최고점 채택 금지 — 이웃 조합까지 통과하는 견고 클러스터만 ③ 파라미터 재보정은 반기 1회 (검증된 결함 발견 시 예외)
> 완료 항목도 지우지 않고 결과와 함께 남긴다. 상세 히스토리는 STRATEGIES.md.

## 진행 중 / 대기
| 순위 | 항목 | 상태 | 비고 |
|---|---|---|---|
| 알림 | 카카오톡 이상·매매·요약 알림 (`KakaoNotifier`) | ✅ **가동 중** (08-10 연결 완료, scope=talk_message 확인) | 나에게 보내기 API (무료). 토큰 자동갱신(액세스 6h/리프레시 2개월 — 일일 요약 발송이 keep-alive 겸함), 분당 10건 폭주 가드, link 필수 필드 박제. 이벤트: engine.start(워치독 부활 감지), reconcile.drift, fill_poll.degraded(장중 5연속 실패), daily.summary(15:40), 기존 scanner.joined·control 전부. Discord 병행 유지. **연결 시 밟은 함정 4종**(재현 대비): ① Redirect URI 등록 위치가 개편 콘솔에선 [앱]>[플랫폼 키]>[REST API 키] 상세 (미등록=KOE006) ② 신규 REST API 키는 클라이언트 시크릿 기본 활성 (누락=KOE010, 별도 [보안] 메뉴 없음) ③ 동의항목이 '선택 동의'면 승인 화면 체크박스 기본 해제 → scope 없는 토큰이 오류 없이 발급되고 발송 단계 403(-402)에서야 발각 → '이용 중 동의'로 해결 ④ 카카오 에러 본문(KOE코드)을 raise_for_status 가 버려 진단 불가 → 본문 노출로 수정 |

| F | **생존편향 정량화** — 상폐 목록(4,164종목)으로 유니버스 오염도 측정, 기대수익 보정 | ✅ 완료 (07-22, commit 1597cd8) — 문서 갱신 누락분 정정 | 재난성 상폐 75건 중 선별 필터 통과 0건(성분①≈0), 승자 풀 착시는 기대치 10~20% 할인(성분②), 장기 거래정지(성분③)는 select-universe DART 경고로 대응. 남은 심화: 상폐 종목 가격 포함 '진짜' 백테스트 (FDR로 가능 확인) |
| D↑ | 팩터(가치·우량) 분기 리밸런스 | ✅ **구현 완료** (08-03, `trader factor-picks` — 분기마다 실행해 수동 리밸런스, `--budget`으로 수량 계산) | **사전 등록 가설**: "시총 상위 200 중 저PBR + 흑자(퀄리티) 상위 5~10종목 동일가중·분기 리밸런스는 2016~2026(하락장 포함)에 비용 차감 후 플러스이고, 눌림목과 월수익 상관이 낮다(<0.5)." 검증 계획: 10년 워크포워드 + 상폐 대조(저PBR엔 부실주 위험↑) + 상관 분석. 검증 결과: (a) 10년 +353%(코스피 +330%), 최악 해 -4%, 2022 하락장 0% ✓ (b) 눌림목과 월상관 **-0.01** ✓✓. 후속가설(레짐 오버레이)은 기각(+353→+90% 붕괴 — 가치주 수익은 회복 초입에 몰림). MDD 46%는 슬리브 배분 20~30%로 관리. 한계: 승자 풀 편향(10~20% 할인), 주식수 현재값 근사 |
| 5 | 진짜 모의투자 전환 (`--live-exec`) | ✅ **완료** (08-03 장중 전환 — 유저 지시) | 보유 0 상태라 청산 절차 불필요. 시작 reconcile in_sync(잔고 TR 실검증), 해외 체결내역 TR은 모의 도메인 500 → read 격리로 국내 무영향화. run_paper.sh 기본 --live-exec |
| B | 스캐너 실순위 스냅샷 사후검증 — data/rankings 축적분으로 시뮬 재현 | ✅ **1차 완료** (08-03, `scripts/replay_rankings.py` — 표본 축적 계속, 월 단위 재실행) | 7거래일 재현: **합류 판정 8/8 초 단위 일치**(순위 스냅샷이 합류를 완전 결정, favorites 완화 영향 0), 당일 진입/관망 방향 6/6 일치. **발견: 미진입 랏이 며칠 뒤 진입**(7/23 합류→7/27 진입 등 2건 합계 -3.6만 — 검증된 시뮬에 없는 동작) → 당일 만료(`scan_lot.expired`)로 라이브를 검증 의미에 정렬. 성적 판정은 표본 부족(체결 2건, 재현 -4.7만 vs 실제 -10.8만) — 스냅샷·EOD 캐시 자동 축적 중이라 매월 재실행 |
| C | 체결강도 등 수급 데이터 검토 | 💤 아이디어 | |
| E | ~~미국 확장~~ | ⛔ 보류 (유저 지시: 한국 집중) | 대형주 10종목 10년 +53%(기본 수수료)·10/10 플러스 — 단 사후편향 미해소, 양도세 22%·환율·야간운영 이슈 |

## 가설 판정 (2026-07-23 사전등록 → 검증 완료)
| 가설 | 판정 | 결과 |
|---|---|---|
| H1 폭락 모멘텀 (코스피 -3% 마감 → 익일 인버스, 3일 보유) | ✅ **구현 완료** (08-03, `crash_momentum_v1` — 114800@1D 자본 5% 소액 관찰 가동) | 10년 34회, 회당 +0.4%(1x)/+1.0%(2x), 누적 +10~18% — 유일하게 플러스인 숏. 얇은 엣지·최악 1회 -20%라 소액 전용 |
| H2 배당 캐리 | ❌ 기각 (기준 80% 미달) | 12개월 롤링 플러스 65% — 지수(58%)보단 낫지만 팩터(71%/+10.3%)의 하위호환 |
| H3 자사주 공시 이벤트 | ❌ 기각 | 절대 +10.5%지만 시장 대비 **-6.0%p** (초과승률 39%) — 강세장 착시 |
| 페어 트레이딩 | ⛔ 검증 불가 | 숏 레그 제도 불가 (신용대주 제한) |

롤링 재채점 (신규 표준): 팩터 12개월 구간 71% 플러스/중앙값 +10.3% (코스피 59%/+4.4%) — 채택 유지.

## 완료 (검증 결과와 판정)
| 순위 | 항목 | 판정 | 근거 |
|---|---|---|---|
| 보안 | **취약점 전수 점검 (2026-09-25, 이슈 #1~#23)** | ✅ 수정 적용 (전략·사이징 로직 변경 없음) | PoC/재현으로 확인한 것만 수정. **대시보드**: 공개 기본 시크릿으로 JWT 위조 → 원격 전량청산 가능했음(#1), CORS `*`+credentials 로 임의 웹페이지가 브라우저 경유 조작 가능(#2) → 기본값 기동 거부·CORS 명시 목록·WS 인증·로그인 제한. **체결 동기화**: 재시작 전 주문 체결이 메모리 랏에 미반영 → 중복 매수·손절 없는 보유(#3), 취소 직전 체결분 무시로 손절 매도 영구 거부(#4), 청산 뒤 매수 잔량이 관리 밖 주식(#5). **주문 수명**: 전일 미체결 영구 잠금(#6), 명시 거부도 UNKNOWN(#11), 주문번호 날짜 미구분(#15), 체결조회 1페이지만(#16). **리스크**: 킬스위치 미영속(#7), 잔고 조회 실패 시 그 봉 손절 스킵(#8), 미체결 매수가 한도 우회(#9), 일일손실·최고평가금 재시작 초기화(#10), dry_run 무효(#13), D+0 예수금 이중계상(#17). **기타**: 웹후크·DART 키 로그 노출(#12), 깨진 WS 틱에 연결 끊김(#18), 카카오 토큰 0644·OAuth state 없음(#19), 스크립트(#21), 페이퍼 대기 지정가 옵트인(#22, 기본 꺼짐), zip 상한·lock(#23). 호가단위 2,000원 미만 5원은 **의도된 보수 설계**(ETF 겸용)라 결함 아님. 남은 확인: KIS 포털에서 `prvs_rcdl_excc_amt`·연속조회 `tr_cont` 실측 |
| G | 숏 전략 (롱숏 반반) | ❌ **전면 기각** | 지수숏 반반 -3%(알파 소멸), 약체종목 숏 -76%/MDD 83%(적자·고PBR=테마주 폭등이 한국 구조), 인버스 반반 -8%. 하락 대응은 레짐 게이트+무상관 조합이 승자. API 확인: 신용대주·선물 주문 API는 존재(계좌·제도 요건 별도) — 지수선물 조건부 헤지만 미래 검토 |
| 2+ | 종목선별 개선 스윕 (랭킹·종목수·주기·유니버스·변동상한 14변형) | **현행 유지** — 전 변형 열세 | 현행 +541만 vs 변형 +246~542만; 변동상한 완화(유저 가설)는 공정 검증에서 반토막 — 사후 편향 확인 |
| 3b | 주문 정정/취소 (EXIT가 미체결 익절 주문 취소 후 발주) | 적용 (UNKNOWN 주문은 안전하게 잠금 유지) | 실전 지정가에서 손절 막힘 사고 방지 |
| A | DART 위험공시 필터 | **기본 꺼짐** — 체커·배선만 완성 | 174건 중 위험공시 5건뿐, 합계 +3.5만이라 켰다면 손해; 호재성 거래정지 오탐 존재 |
| 1 | 시장 레짐 필터 | **ORB만 적용**, 스캐너 기각 | ORB +44/+27만 개선 vs 스캐너 -100만 손해 |
| 2 | 종목선별 자동화 (`trader select-universe`) | 도구로 채택, 운용 종목 유지 | 워크포워드 +607만, 11/15분기 플러스, 나쁜 장 자동 관망 |
| 3a | 손절 수량에 비용 여유 (사이징 버퍼) | 적용 (스캐너 0.33%, ETF 0.13%) | 손절 실손실 -7.2만→-6.1만; 총수익 -19만 대가 수용 |
| 4 | 부분익절 재설계 | **현행 유지** | 빠른익절 -32만 손해, 익절없음은 승률 39%로 급락 |
| 6 | 데이터 자산 쌓기 | 적용 | EOD 분봉 자동 캐시 + 순위 스냅샷 기록 중 |
| 🚨 | **매도 주문 100% 실패 = 손절 불능 (8일간)** | ✅ **수정·라이브 검증 완료** (08-11) | 실주문 전환(08-03) 이후 매도 체결 0건. DB 대조: 매도 17건은 전부 7월 시뮬 구간. `SLL_TYPE="01"` 이 `IGW00007 "MCA 전문바디 구성 중 오류"` 를 유발 — KIS 공식 매도 예시(chk_order_cash.py)는 sll_type 을 넘기지 않아 `""` 로 나간다. **손절 신호가 실제로 뜬 08-11 아침에야 발각** — 그 사이 봇은 청산 불능 상태로 운용됐다. 라이브 검증: 매도 수용(0000024847)+취소 정상. 곁가지로 CNDT_PRIC 누락도 함께 정정(공식 키 집합 일치) |
| 🛡 | **`trader selfcheck` — 라이브 기본기능 상시 점검** | ✅ 신설 (08-11, 11/11 통과) | "기회가 왔을 때 알아채는 구조는 너무 늦다"(유저). 인증·잔고·체결내역·주문내역·분봉·순위·현재가·**매수/매도 주문+취소**·환전을 비파괴로 점검 (체결 불가 지정가 1주 → 즉시 취소). 거부 사유를 구분해 '전문 형식 오류'만 실패로, '잔고/수량 부족'은 통과로 본다(계약은 정상). 봇 가동 중 실행 대비 절반 속도. **점검 자체가 3개 결함을 추가 발견**: ① KisAuth 중복 생성 → 토큰 발급 403 ② 모의 도메인은 순위 TR 미지원(실전 키로 조회해야) ③ 호가단위 미정렬 |
| 🐞 | **호가단위 미정렬 주문 거부 위험** | ✅ 수정 (08-11) | `호가단위 오류` 로 거부. 정상 경로는 시세를 그대로 써서 유효하지만, **시세 미수신 시 폴백인 평단가는 소수점**이라 손절이 막힐 수 있었다. `execution/tick_size.py` 로 정렬(매수 올림/매도 내림 — 미체결보다 한 틱 불리가 낫다). ⚠️ 정렬을 주문 직전에 하면 매수 올림이 `max_order_notional` 을 깨서(테스트가 검출) **사이징 전에** 정렬하도록 배치 |
| 🐞 | **시세 재접속 폭증 (219회/일) — DNS 고갈 + 국소 실패의 전면 증폭** | ✅ **수정** (08-10) | 로그 전수 분석: WS 자체 오류는 18건뿐, **194건은 REST 오류가 `process()` 를 타고 올라와 WS 를 무너뜨린 것**. 그 중 161건이 `socket.gaierror`(DNS 실패). **원인**: 모든 REST 호출이 호출마다 새 `httpx.AsyncClient` 생성 → 매번 DNS+TLS. 실측 호출량 하루 ~29,000회(체결폴링 2초 + 상태스냅샷 잔고조회 5초×24h)로 macOS 리졸버가 실패. **수정**: ① `infra/http.py` 루프별 공용 클라이언트(keep-alive 풀) — DNS·핸드셰이크가 호출당→커넥션당 ② `service.run` 이 봉 1개 실패를 격리(국소 REST 장애가 재접속 5초 동안 전 종목 손절·익절 정지로 증폭되던 구조 차단), `health.process_errors` 로 누적 노출(조용히 삼키지 않음). 검증: 신규 테스트 7종 + 재가동 후 에러 0. **관찰 필요**: 내일 장중 재접속 횟수가 실제로 떨어지는지 |
| 🐞 | **스캐너 합류 종목 시세 유실 (구독 목록 정적 관리)** | ✅ **수정** (08-10) | **오래된 조용한 고장 — 스캐너 전략이 사실상 무력했을 가능성.** `_join` 이 합류분을 현재 WS 연결에만 구독하고 `tickers` 에는 안 넣어, 다음 재접속에서 시세가 영구 유실. 08-10 실측: 유니켐 09:06 합류 → 09:12 첫 재접속에 유실 → 6시간 반 무시세 → 진입 불가(차단 로그 0 = 리스크 관리 문제 아님). 재접속이 7월 55~82회/일, 08-10 **219회**(평균 1.8분)라 사실상 매번 즉시 유실. **2차 발견**: 복원 경로도 `open_tickers()`(보유)만 봐서 **관망 랏 12 중 6이 미구독**, 봉이 없으니 당일 만료 판정도 못 돌아 랏이 영구 잔존(악순환). **수정**: 구독 목록을 정적 리스트가 아니라 `service.tracked_tickers()`(템플릿 보유 + **살아있는 랏 전부**)에서 매 접속마다 파생 → 합류분 유실·만료분 누적·구독한도 과소계상 3건 동시 해소. 검증: 신규 테스트 5종 + 재가동 시 구독 6→13 실측 |
| - | 대시보드 건강 상태 + 시세 끊김 워치독 (`EngineHealth`) | 적용 (08-10) | **조용한 고장 차단**: 기존엔 상태 파일 갱신(=프로세스 생존)만 봐서, WS 는 붙어 있는데 데이터가 안 오는 경우(구독 실패·서버 무응답) 화면·알림 모두 정상으로 보이고 전략은 영원히 관망만 했다. 이제 마지막 봉 수신 시각/티커, 누적 봉·재접속 횟수, 마지막 체결폴링 성공·연속 실패를 스냅샷에 싣고 대시보드에 표시. 장중 5분 무소식이면 `feed.stale` 카카오 알림, 회복 시 `feed.recovered`. **장외 무소식은 정상 처리**(주말 거짓 경보가 알림 신뢰를 무너뜨리므로). 검증: 단위테스트 12종(세션 안팎 경계·임계·UTC 환산·직렬화) + 재가동 후 스냅샷 실측 |
| - | KRX 휴장일 캘린더 (`KRX_HOLIDAYS`) + 검증 스크립트 | 적용 (08-10) | 공휴일 폴링 완전 침묵. **위험 비대칭 명시**: 휴장일 누락은 무해(백오프가 누름), 거래일 오등재는 그날 체결 폴링이 멈춰 위험 → 확신 없으면 빼는 규칙. `scripts/verify_holidays.py` 가 FDR 실거래일과 대조해 "거래일 오등재 0" 을 게이트. **초안 실측에서 8/17(광복절 대체) 누락·7/17 누락을 잡아 정정** (거래일 오등재는 0이었음). 표는 평일만 수록(주말 중복 제거), 미갱신 연도는 평일 취급으로 안전 열화 |
| - | 체결 폴링 안정화 — 장외 게이트 + 실패 백오프 (`PollGate`) | 적용 (08-08) | 장외 fill_poll 500 에러 956건 누적(시간 분포 분석: 전부 장외, 장중 0건 — VTS가 장외에 체결내역 TR 거부). 평일 08:30~16:30만 폴링 + 연속 실패 시 지수 백오프(상한 60s). 검증: 단위테스트 9종(세션 경계·백오프 진행·리셋) + 재시작 후 주말 `session_idle` 진입·에러 0건 실측. 잔여: 공휴일은 평일 취급(백오프가 60s로 완화) — KRX 휴장일 캘린더는 추후 |
| - | 눌림목 1D 가드 (10년 재검증) | 적용 | 10년 기준 수익 동일 + MDD 16→12/21→15% |
